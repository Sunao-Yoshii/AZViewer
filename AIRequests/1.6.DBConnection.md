# 実装設計書

## AZViewer DB接続管理の SQLAlchemy Core 移行

## 1. 目的

AZViewer の SQLite 接続管理方式を見直し、従来の `sqlite3.Connection` 共有方式から、SQLAlchemy Core の `Engine` 共有方式へ移行する。

これにより、以下を実現する。

```text
- 単一 sqlite3.Connection 共有によるスレッド干渉を避ける
- Repository 層から Connection の寿命管理を分離する
- SQL 実行単位ごとに短い Connection / Transaction を使う
- SELECT と更新処理の接続利用方針を明確化する
- SQLite の WAL / busy_timeout / foreign_keys を明示設定する
- 長時間 transaction を避け、DB ロック問題を起こしにくくする
```

本変更では ORM は導入しない。

使用するのは SQLAlchemy Core の以下に限定する。

```text
- create_engine
- Engine
- Connection
- text()
- engine.connect()
- engine.begin()
```

## 2. 背景

従来は、アプリケーション全体で `sqlite3.Connection` を保持し、Repository へ渡す構成だった。

しかし、複数スレッドや複数処理経路から DB へアクセスする場面が増えたことで、以下の問題が起きやすくなっている。

```text
- 同じ sqlite3.Connection を複数スレッドで共有する危険
- check_same_thread=False による制限解除後の排他制御不足
- 書き込み transaction 中の SELECT 停止
- commit / rollback / cursor ライフサイクルの干渉
- database is locked 系エラー
- 長時間 transaction による UI 操作待ち
```

AZViewer は現在、画像検索、詳細更新、タグ更新、モデル更新、caption 読み込み、ワイルドカード出力、マスタメンテナンス、ファイル移動、ごみ箱移動、起動時整合性確認など、DB 利用箇所が増えている。

そのため、単一 Connection を大事に抱える方式はやめる。

接続を抱きしめすぎると、愛ではなくロックになります。

## 3. 基本方針

## 3.1 採用方針

```text
- SQLAlchemy Core を採用する
- ORM は使わない
- Repository は sqlite3.Connection ではなく Engine を受け取る
- Engine は DatabaseLifecycleManager が生成・保持する
- SELECT は原則 with engine.connect()
- INSERT / UPDATE / DELETE は原則 with engine.begin()
- 複数テーブルを整合更新する処理は、上位で transaction を1つだけ開く
- Repository 内部メソッドは必要に応じて Connection を受け取れるようにする
- poolclass は NullPool
- PRAGMA foreign_keys = ON
- PRAGMA busy_timeout = 30000
- PRAGMA journal_mode = WAL
- journal_mode=WAL は初期化時に一度設定し、結果を確認する
- 長時間 transaction を持たない
- ファイル操作・画像処理・メタデータ読み取り中は transaction を保持しない
- 重い一括処理中は既存方針どおり UI ロックする
```

## 3.2 避ける方針

```text
- 単一 sqlite3.Connection をアプリ全体で共有する
- check_same_thread=False にして共有 Connection を使い続ける
- SQLAlchemy StaticPool で単一 Connection 共有へ寄せる
- ファイル操作中に DB transaction を保持する
- 画像処理中に DB transaction を保持する
- メタデータ読み取り中に DB transaction を保持する
- 長い一括処理全体を1つの transaction にする
```

## 4. 対象ファイル

## 4.1 変更対象

```text
backend/requirements.txt
backend/api/database_lifecycle_manager.py
backend/api/app_api.py
backend/api/app_lifecycle_api.py
backend/api/image_catalog_api.py
backend/api/image_register_api.py
backend/repositories/image_file_repository.py
backend/services/image_file_import_service.py
backend/services/startup_cleanup_service.py
backend/services/prompt_tag_import_service.py
backend/services/caption_tag_import_service.py
backend/services/bulk_tag_add_service.py
backend/services/tag_caption_export_service.py
backend/services/thumbnail_cache_service.py
backend/services/wildcard_export_service.py
```

## 4.2 原則変更不要

```text
frontend/
```

本変更はバックエンド内部の DB 接続管理方式変更であり、API 入出力仕様は変更しない。

フロントエンドの payload / response / UI 挙動は維持する。

## 5. 依存関係追加

## 5.1 backend/requirements.txt

SQLAlchemy を追加する。

```text
SQLAlchemy
```

バージョン固定を行う場合は、現行環境で検証したバージョンに合わせる。

例。

```text
SQLAlchemy>=2.0,<3.0
```

## 5.2 PyInstaller 対応

SQLAlchemy は通常の pure Python 依存として扱える想定だが、配布ビルド時に import error が出ないことを確認する。

必要がある場合のみ、PyInstaller の collect 対象へ追加する。

```text
- AZViewer.spec
- build_windows.ps1
```

ただし、まずは明示 collect なしで動作確認する。

## 6. DatabaseLifecycleManager 仕様変更

## 6.1 役割

`DatabaseLifecycleManager` は、SQLite Connection ではなく SQLAlchemy Engine のライフサイクルを管理する。

## 6.2 変更前の責務

```text
- sqlite3.Connection を生成する
- sqlite3.Connection を保持する
- get_connection() で Connection を返す
- close() で Connection を閉じる
```

## 6.3 変更後の責務

```text
- DB ファイルパスを解決する
- SQLAlchemy Engine を生成する
- Engine を保持する
- Engine を返す
- SQLite 初期 PRAGMA を設定する
- アプリ終了時に engine.dispose() する
```

## 6.4 持たせない責務

```text
- Repository インスタンス生成
- Service インスタンス生成
- API インスタンス生成
- 起動時 cleanup の業務処理本体
- tag_hash 補完処理本体
- thumbnail 補完処理本体
```

これらは既存通り `AppLifecycleApi` や各 Service に置く。

`DatabaseLifecycleManager` に何でも持たせ始めると、名前は Lifecycle なのに中身は町内会長になります。

## 6.5 公開インターフェース

```python
from sqlalchemy.engine import Engine


class DatabaseLifecycleManager:
    def initialize(self) -> None:
        ...

    def get_engine(self) -> Engine:
        ...

    def dispose(self) -> None:
        ...
```

互換用に一時的に `close()` を残してもよい。

```python
def close(self) -> None:
    self.dispose()
```

ただし、新規実装では `dispose()` を使用する。

## 6.6 get_connection() の扱い

`get_connection()` は廃止する。

移行途中の互換用として残す場合でも、最終的には全参照を削除する。

```text
非推奨:
database_lifecycle_manager.get_connection()

推奨:
database_lifecycle_manager.get_engine()
```

## 7. Engine 作成仕様

## 7.1 新規関数

`database_lifecycle_manager.py` 内、または `backend/db/database_engine.py` を新設して、Engine 作成処理を分離する。

推奨は新規ファイル追加。

```text
backend/db/database_engine.py
```

## 7.2 create_database_engine

```python
from __future__ import annotations

from pathlib import Path

from sqlalchemy import create_engine, event, text
from sqlalchemy.engine import Engine
from sqlalchemy.pool import NullPool


def create_database_engine(db_path: str | Path) -> Engine:
    db_file = Path(db_path)
    db_file.parent.mkdir(parents=True, exist_ok=True)

    engine = create_engine(
        f"sqlite:///{db_file.as_posix()}",
        connect_args={
            "timeout": 30,
        },
        poolclass=NullPool,
        future=True,
    )

    @event.listens_for(engine, "connect")
    def set_sqlite_pragma(dbapi_connection, connection_record):
        cursor = dbapi_connection.cursor()
        cursor.execute("PRAGMA foreign_keys = ON")
        cursor.execute("PRAGMA busy_timeout = 30000")
        cursor.close()

    with engine.begin() as conn:
        journal_mode = conn.execute(
            text("PRAGMA journal_mode = WAL")
        ).scalar()

        if str(journal_mode).lower() != "wal":
            raise RuntimeError(
                f"SQLite WAL mode を有効化できませんでした: {journal_mode}"
            )

    return engine
```

## 7.3 PRAGMA 方針

## 7.3.1 connect event で毎回設定するもの

```sql
PRAGMA foreign_keys = ON;
PRAGMA busy_timeout = 30000;
```

理由。

```text
- foreign_keys は Connection ごとの設定であるため
- busy_timeout も Connection ごとの設定として明示するため
```

## 7.3.2 初期化時に一度設定するもの

```sql
PRAGMA journal_mode = WAL;
```

理由。

```text
- journal_mode は DB ファイル側に残る設定であるため
- 毎 Connection で実行する必要は薄いため
- 設定結果が wal になったことを起動時に確認したいため
```

## 8. DatabaseLifecycleManager 実装例

```python
from __future__ import annotations

from pathlib import Path

from sqlalchemy.engine import Engine

from backend.db.database_engine import create_database_engine


class DatabaseLifecycleManager:
    """AZViewer の SQLite Engine ライフサイクルを管理する。

    Repository / Service / API インスタンスは保持しない。
    DB 接続方式の詳細をアプリケーション各所から隠蔽し、
    SQLAlchemy Engine の生成、取得、破棄のみを担当する。
    """

    def __init__(self, db_path: str | Path | None = None):
        self._db_path = Path(db_path) if db_path else self._resolve_default_db_path()
        self._engine: Engine | None = None

    def initialize(self) -> None:
        if self._engine is not None:
            return

        self._engine = create_database_engine(self._db_path)

    def get_engine(self) -> Engine:
        if self._engine is None:
            self.initialize()

        if self._engine is None:
            raise RuntimeError("Database engine が初期化されていません。")

        return self._engine

    def dispose(self) -> None:
        if self._engine is None:
            return

        self._engine.dispose()
        self._engine = None

    def close(self) -> None:
        self.dispose()

    def _resolve_default_db_path(self) -> Path:
        return Path("data") / "azviewer.db"
```

## 9. AppApi 変更

## 9.1 終了処理

`AppApi` またはアプリ終了フックで、従来の `close()` 呼び出しを `dispose()` へ変更する。

互換のため `close()` を残す場合は、既存呼び出しはそのままでもよい。

```python
def dispose(self):
    self._database_lifecycle_manager.dispose()
```

## 9.2 専門 API への注入

既存通り `DatabaseLifecycleManager` を各 API へ渡す。

```python
self._database_lifecycle_manager = DatabaseLifecycleManager()

self._image_catalog_api = ImageCatalogApi(
    database_lifecycle_manager=self._database_lifecycle_manager,
    ...
)

self._image_register_api = ImageRegisterApi(
    database_lifecycle_manager=self._database_lifecycle_manager,
    ...
)

self._app_lifecycle_api = AppLifecycleApi(
    database_lifecycle_manager=self._database_lifecycle_manager,
    ...
)
```

各 API は必要時に `get_engine()` を呼ぶ。

## 10. Repository 実装方針

## 10.1 コンストラクタ

`ImageFileRepository` は `sqlite3.Connection` ではなく `Engine` を受け取る。

```python
from sqlalchemy.engine import Engine


class ImageFileRepository:
    def __init__(self, engine: Engine):
        self.engine = engine
```

## 10.2 単発 SELECT

単発 SELECT は Repository メソッド内で `engine.connect()` を使う。

```python
from sqlalchemy import text


def find_by_id(self, image_id: int):
    with self.engine.connect() as conn:
        return conn.execute(
            text("""
                SELECT *
                FROM image_file_data
                WHERE id = :image_id
            """),
            {"image_id": image_id},
        ).mappings().first()
```

## 10.3 単発更新

単一 SQL または単一意味単位で完結する更新は `engine.begin()` を使う。

```python
def update_comment(self, image_id: int, comment: str | None) -> None:
    with self.engine.begin() as conn:
        conn.execute(
            text("""
                UPDATE image_file_data
                SET comment = :comment
                WHERE id = :image_id
            """),
            {
                "image_id": image_id,
                "comment": comment,
            },
        )
```

## 10.4 複数テーブル整合更新

複数テーブルをまとめて更新する処理は、上位メソッドで `engine.begin()` を1回だけ開く。

内部メソッドは `conn` を受け取る。

```python
def update_detail_with_tags_and_model(
    self,
    detail,
    tags: list[str],
    model_name: str | None,
) -> None:
    with self.engine.begin() as conn:
        self._update_detail(conn, detail)
        self._replace_tags(conn, detail.id, tags)
        self._replace_model(conn, detail.id, model_name)
        self._replace_tag_hash(conn, detail.id)
```

内部メソッド例。

```python
def _update_detail(self, conn, detail) -> None:
    conn.execute(
        text("""
            UPDATE image_file_data
            SET
                rating = :rating,
                is_checked = :is_checked,
                is_favorite = :is_favorite,
                comment = :comment
            WHERE id = :id
        """),
        {
            "id": detail.id,
            "rating": detail.rating,
            "is_checked": detail.is_checked,
            "is_favorite": detail.is_favorite,
            "comment": detail.comment,
        },
    )
```

## 10.5 実装ルール

```text
- public メソッドは原則 engine.connect() / engine.begin() を使ってよい
- ただし、複合更新に使われる処理は private メソッド化し、conn を受け取る
- private メソッド内では commit / rollback を行わない
- private メソッド内では engine.begin() を開かない
- transaction の境界は上位 public メソッドで決める
```

## 10.6 SQL 記述方針

`?` プレースホルダをやめ、SQLAlchemy の named parameter を使う。

変更前。

```python
cursor.execute(
    "SELECT * FROM image_file_data WHERE id = ?",
    (image_id,),
)
```

変更後。

```python
conn.execute(
    text("""
        SELECT *
        FROM image_file_data
        WHERE id = :image_id
    """),
    {"image_id": image_id},
)
```

## 11. Row 取得方針

## 11.1 mappings() を使う

SQLAlchemy の `Row` を辞書風に扱う場合は `.mappings()` を使う。

```python
rows = conn.execute(
    text("""
        SELECT id, filename, path
        FROM image_file_data
        ORDER BY id DESC
    """)
).mappings().all()
```

## 11.2 DTO 変換

既存 DTO 変換処理は維持する。

```python
def _row_to_item(self, row) -> ImageFileListItem:
    return ImageFileListItem(
        id=row["id"],
        filename=row["filename"],
        path=row["path"],
        ...
    )
```

## 12. IN 句の扱い

SQLAlchemy Core の `text()` で可変長 `IN` 句を扱う場合は、`bindparam(expanding=True)` を使う。

```python
from sqlalchemy import bindparam, text


stmt = text("""
    SELECT *
    FROM image_file_data
    WHERE id IN :ids
""").bindparams(bindparam("ids", expanding=True))

rows = conn.execute(stmt, {"ids": image_ids}).mappings().all()
```

空配列の場合は SQL 実行前に戻す。

```python
if not image_ids:
    return []
```

## 13. Schema 初期化方針

## 13.1 create_table

`ImageFileRepository.create_table()` は `engine.begin()` で実行する。

```python
def create_table(self) -> None:
    with self.engine.begin() as conn:
        conn.execute(text("""
            CREATE TABLE IF NOT EXISTS image_file_data (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                filename TEXT NOT NULL,
                path TEXT NOT NULL UNIQUE,
                folder TEXT NOT NULL,
                rating TEXT NOT NULL CHECK (rating IN ('General', 'R-15', 'R-18', 'R-18G')),
                is_checked INTEGER NOT NULL CHECK (is_checked IN (0, 1)),
                is_favorite INTEGER NOT NULL CHECK (is_favorite IN (0, 1)),
                comment TEXT NULL
            )
        """))

        ...
```

## 13.2 AppLifecycleApi

`AppLifecycleApi.bootstrap_app()` の既存責務は維持する。

```text
- DB 初期化
- テーブル作成
- 起動時 cleanup
- tag_hash 補完
- thumbnail 補完
- 初期検索
```

ただし、内部で Repository を生成する際は `get_engine()` を使う。

```python
repository = ImageFileRepository(
    self._database_lifecycle_manager.get_engine()
)
```

## 14. Service 層変更方針

## 14.1 Service は Engine を直接持たない

原則として、既存通り Service は Repository を使う。

ただし、Service 内で Repository を生成している場合は、`get_connection()` ではなく `get_engine()` を渡す。

変更前。

```python
repository = ImageFileRepository(
    self._database_lifecycle_manager.get_connection()
)
```

変更後。

```python
repository = ImageFileRepository(
    self._database_lifecycle_manager.get_engine()
)
```

## 14.2 一括処理の transaction 粒度

一括処理全体を1 transaction にしない。

対象。

```text
- prompt タグ読み取り
- caption タグ読み込み
- 一括タグ追加
- サムネイル補完
- 起動時 cleanup
- ファイル移動
- ごみ箱移動
```

基本方針。

```text
- 1画像単位または小さなまとまりで DB 更新する
- 1件失敗しても継続可能な処理は、個別に失敗扱いにする
- ファイル操作を DB transaction 内に入れない
```

## 15. ファイル操作を伴う処理の方針

## 15.1 ごみ箱移動

処理順序。

```text
1. DB から対象画像情報を取得する
2. DB Connection を閉じる
3. 実画像をごみ箱へ移動する
4. 成功した ID を集計する
5. 成功分だけ DB 管理対象から除外する
```

避けること。

```text
- DB transaction を開いたまま send2trash を実行する
```

## 15.2 ファイル移動

処理順序。

```text
1. DB から対象画像情報を取得する
2. DB Connection を閉じる
3. 実ファイルを移動する
4. 成功したファイルだけ path / folder 更新する
```

## 15.3 リネーム

処理順序。

```text
1. DB から対象画像情報を取得する
2. DB Connection を閉じる
3. 実ファイルをリネームする
4. DB の filename / path / folder を更新する
5. DB 更新に失敗した場合は、可能な範囲で実ファイル名を戻す
```

## 15.4 サムネイル生成

処理順序。

```text
1. DB から対象画像情報を取得する
2. DB Connection を閉じる
3. Pillow でサムネイル生成する
4. 必要なら生成結果だけ DB / 結果集計へ反映する
```

Pillow 処理中に transaction を持たない。

画像処理中に DB ロックを握るのは、レジで財布を出したまま昼寝するようなものです。

## 16. ImageCatalogApi 変更方針

## 16.1 Repository 取得

`_get_repository()` がある場合は、Engine を渡す形へ変更する。

```python
def _get_repository(self) -> ImageFileRepository:
    return ImageFileRepository(
        self._database_lifecycle_manager.get_engine()
    )
```

Repository を都度生成してよい。

`Engine` は共有されるため、Repository インスタンス自体を長期保持する必要はない。

## 16.2 update_image_file_detail

詳細、タグ、モデル、tag_hash を同一 transaction で更新する。

Repository 側に複合更新メソッドを用意する。

```python
repository.update_detail_with_tags_and_model(
    detail=detail,
    tags=tags,
    model_name=model_name,
)
```

## 16.3 remove_image_files_from_catalog

管理対象除外は DB 内の関連テーブル削除とサムネイルキャッシュ削除が絡む。

推奨順序。

```text
1. DB から対象情報を取得
2. DB transaction で関連レコード削除
3. transaction 成功後にサムネイルキャッシュ削除
```

サムネイル削除失敗は、DB 削除 rollback の理由にしない。

キャッシュはキャッシュであり、王族ではない。

## 17. ImageRegisterApi / ImageFileImportService 変更方針

## 17.1 登録処理

画像登録時は、DB 登録とサムネイル生成を分ける。

推奨順序。

```text
1. ファイル走査
2. 既存登録済み path を DB から取得
3. 新規候補を作成
4. DB へ登録
5. 登録成功したレコードを取得
6. DB transaction 外でサムネイル生成
```

## 17.2 insert_many

`insert_many` は `engine.begin()` を使う。

大量登録時に1件ずつ transaction を開くと遅くなるため、1回の登録操作内ではまとめてよい。

ただし、画像読み込みやサムネイル生成は含めない。

## 18. StartupCleanupService 変更方針

## 18.1 処理順序

```text
1. DB から登録済み path 一覧を取得
2. DB Connection を閉じる
3. 実ファイル存在確認を行う
4. 不存在 ID を集計する
5. DB transaction で管理対象から除外する
6. transaction 成功後にサムネイルキャッシュを削除する
```

実ファイル存在確認中に transaction を保持しない。

## 19. エラー処理

## 19.1 database is locked

`database is locked` が出た場合、まず以下を疑う。

```text
- 長時間 transaction を保持していないか
- ファイル操作中に transaction を保持していないか
- 画像処理中に transaction を保持していないか
- 複数 writer を同時に走らせていないか
- UI ロック対象の重処理で多重実行を許していないか
```

安易に busy_timeout を延ばして隠さない。

timeout を伸ばすだけの対策は、火災報知器の音量を下げるようなものです。

## 19.2 ログ出力

DB 更新失敗時は、最低限以下をログへ残す。

```text
- 対象 API / Service
- 対象処理名
- 対象 ID 数
- 例外種別
- 例外メッセージ
```

SQL全文や個人パスを過剰に UI 表示しない。

## 20. テスト観点

## 20.1 起動

```text
- 初回起動で DB ファイルが作成されること
- テーブルが作成されること
- WAL mode が有効になること
- foreign_keys が有効になること
- busy_timeout が設定されること
- 起動時 cleanup が正常に完了すること
```

## 20.2 検索

```text
- 画像一覧検索が動作すること
- タグ検索が動作すること
- フォルダ検索が動作すること
- モデル検索が動作すること
- 重複タグ構成検索が動作すること
- ページング、ソートが従来通り動作すること
```

## 20.3 更新

```text
- 詳細更新が動作すること
- タグ更新が動作すること
- モデル更新が動作すること
- tag_hash が更新されること
- 途中失敗時に複合更新が rollback されること
```

## 20.4 一括処理

```text
- プロンプト読み取りが動作すること
- caption タグ読み込みが動作すること
- 一括タグ追加が動作すること
- 一括属性更新が動作すること
- マスタメンテナンスが動作すること
```

## 20.5 ファイル操作

```text
- 管理対象から除外が動作すること
- ごみ箱移動が動作すること
- ファイル移動が動作すること
- リネームが動作すること
- DB 更新失敗時のリネーム戻し処理が従来通り動作すること
```

## 20.6 並行操作

```text
- 検索中に別検索が多重実行されないこと
- 重い処理中に UI ロックされること
- ごみ箱移動中に別の DB 更新操作が走らないこと
- database is locked が再現しないこと
```

## 21. 受け入れ条件

## 21.1 接続管理

```text
- DatabaseLifecycleManager が Engine を生成・保持すること
- sqlite3.Connection を共有保持しないこと
- get_engine() で Engine を取得できること
- dispose() で Engine を破棄できること
- get_connection() 参照が残っていないこと
```

## 21.2 Repository

```text
- ImageFileRepository が Engine を受け取ること
- sqlite3 の cursor 直接利用が残っていないこと
- SQLAlchemy text() を使っていること
- SELECT は engine.connect() を使うこと
- 更新は engine.begin() を使うこと
- 複合更新は上位で transaction を1つにまとめること
```

## 21.3 Transaction

```text
- ファイル操作中に transaction を保持していないこと
- 画像処理中に transaction を保持していないこと
- メタデータ読み取り中に transaction を保持していないこと
- 長時間一括処理全体を1 transaction にしていないこと
```

## 21.4 既存機能

```text
- 既存のフロントエンド API 呼び出しを変更せずに動作すること
- 画像登録が動作すること
- 画像検索が動作すること
- 詳細編集が動作すること
- タグ/モデル関連機能が動作すること
- ファイル移動/ごみ箱移動/リネームが動作すること
- 起動時整合性確認が動作すること
```

## 22. 実装順序

```text
1. backend/requirements.txt に SQLAlchemy を追加する
2. backend/db/database_engine.py を追加する
3. create_database_engine() を実装する
4. DatabaseLifecycleManager を Engine 管理方式へ変更する
5. get_engine() / dispose() を追加する
6. 互換用 close() を dispose() 委譲にする
7. ImageFileRepository の constructor を Engine 受け取りへ変更する
8. ImageFileRepository の SELECT 系メソッドを engine.connect() + text() へ移行する
9. ImageFileRepository の単発更新系メソッドを engine.begin() + text() へ移行する
10. ImageFileRepository の複合更新系メソッドを conn 受け取り内部メソッドへ整理する
11. AppLifecycleApi の Repository 生成を get_engine() へ変更する
12. ImageCatalogApi の Repository 生成を get_engine() へ変更する
13. ImageRegisterApi / 各 Service の Repository 生成を get_engine() へ変更する
14. get_connection() 参照を全検索して削除する
15. sqlite3 import の不要箇所を削除する
16. 起動確認を行う
17. 検索・詳細更新・タグ更新・モデル更新を確認する
18. 画像登録・サムネイル生成を確認する
19. ごみ箱移動・ファイル移動・リネームを確認する
20. 起動時 cleanup / tag_hash 補完 / thumbnail 補完を確認する
21. 配布ビルドを確認する
```

## 23. 移行時の注意

## 23.1 全面置換を一気にやりすぎない

最初に `DatabaseLifecycleManager` と `ImageFileRepository` の constructor だけ変えて、全メソッドを一気に書き換えると差分が大きくなりすぎる。

推奨は以下。

```text
Phase 1:
- Engine 作成
- DatabaseLifecycleManager 変更
- Repository constructor 変更

Phase 2:
- create_table / search / find 系 SELECT を移行

Phase 3:
- update_detail / replace_tags / replace_model / replace_tag_hash を移行

Phase 4:
- 削除、移動、リネーム、マスタメンテナンスを移行

Phase 5:
- Service 側の get_connection 参照削除
```

## 23.2 row_factory 前提を捨てる

sqlite3 の `row_factory` 前提コードは SQLAlchemy では使わない。

`mappings()` を使う。

## 23.3 lastrowid

INSERT 後の ID 取得は、SQLAlchemy の result を使う。

```python
result = conn.execute(...)
new_id = result.lastrowid
```

ただし、SQLite で期待通り取得できるかは実装時に確認する。

必要なら INSERT 後に UNIQUE key で再 SELECT する。

## 23.4 executescript は使わない

複数 SQL をまとめた `executescript` は使わない。

SQLAlchemy では `conn.execute(text(...))` を SQL 単位で実行する。

## 23.5 PRAGMA の確認

起動時に以下を確認できる簡易ログを出してよい。

```text
- journal_mode
- foreign_keys
- busy_timeout
```

ただし UI 表示は不要。

## 24. 完了後の削除候補

SQLAlchemy 移行が完了したら、以下を確認する。

```text
- backend/db/connection_manager.py が不要になっていないか
- sqlite3 import が残っていないか
- get_connection() が残っていないか
- check_same_thread 設定が残っていないか
```

`backend/db/connection_manager.py` が完全に不要であれば削除する。

ただし、ファイル構成上の削除は移行完了後に行う。

## 25. 最終方針

本変更後の DB アクセス方針は以下とする。

```text
AZViewer は SQLAlchemy Core の Engine をアプリ全体で共有する。
sqlite3.Connection は共有しない。

Repository は Engine を受け取り、SQL 実行時だけ Connection を開く。
SELECT は connect、更新は begin を使う。

複数テーブルを整合更新する場合は、上位で transaction をまとめる。
ファイル操作、画像処理、メタデータ読み取り中は transaction を保持しない。

SQLite は WAL / foreign_keys / busy_timeout を明示設定する。
重い処理は既存方針どおり UI ロックと組み合わせる。
```
